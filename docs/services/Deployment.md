# Deployment

`DeploymentService` — резолв веток для runtime-окружения.

## Методы

- `ResolveBranchAsync(environmentId, version)` → `ResolveBranchResponseDto` — определение ветки по окружению и версии клиента

## Code
- `Core/Services/Deployment/*`
